# 유튜브 알림 — 에이전트 지시서

이 폴더를 연 에이전트(클로드·코덱스)가 읽는 문서입니다.
**사람은 이 문서를 읽지 않습니다.** 사람에게는 결과만 말해주세요.

## 이 폴더가 하는 일

내 유튜브 채널의 구독자·조회수·새 댓글·예약 영상을 **텔레그램으로 보내주는 도구**입니다.
실행은 깃허브(클라우드)가 합니다. 그래서 컴퓨터를 꺼도 알림이 옵니다.

## 사용자에 대해 알아둘 것

- 유튜브를 하는 사람이고, 개발자가 아닙니다. 대부분 윈도우를 씁니다.
- **경로·파일 이름·명령어를 사람에게 보여주지 마세요.** 사람이 할 일은 화면에서
  버튼 누르는 것뿐입니다. 나머지는 에이전트가 직접 하세요.
- 사람에게 "찾아보세요"라고 시키지 마세요. 찾는 것은 에이전트가 합니다.
- 한 번에 한 가지만 부탁하세요. 여러 개를 한꺼번에 시키면 막힙니다.

## 세팅을 시작할 때

사람이 "세팅해줘" 비슷한 말을 하면 `guide/setup.md`를 읽고 그대로 따르세요.
절차를 건너뛰지 말고, 단계마다 확인하고 넘어가세요.

"이어서 세팅해줘"라고 하거나 대화가 끊겼다 다시 왔으면, 처음부터 다시 시키지 말고
`guide/setup.md`의 **"어디까지 했는지 알아내기"** 표로 진행 상태를 먼저 복원하세요.

## 자주 나오는 부탁

**"채널 하나 더 넣어줘"** — 주소를 받아 `python setup.py 채널 @새핸들`. 이 명령은 기존 채널을
지우지 않고 덧붙입니다. 그다음 깃허브에 반영해야 알림에 뜹니다. **한 줄에 하나씩.**

```
git pull --rebase
```
```
git add channels.json
```
```
git commit -m "채널 추가"
```
```
git push
```

(`git pull --rebase`를 먼저 하는 이유: 깃허브가 상태 파일을 스스로 갱신해 두기 때문에
그냥 푸시하면 거부됩니다.)

**"예약 영상도 알려줘"** — `guide/setup.md`의 8단계부터 하면 됩니다.

## 고칠 때 주의할 것

- 열쇠(비밀번호·토큰)는 **절대 파일에 적어 깃허브에 올리지 마세요.** 깃허브 Secrets에만 둡니다.
  로컬 `config.json`과 `oauth_tokens.json`은 올라가지 않게 막혀 있습니다.
- 알림을 한 메시지에 몰지 마세요. 길어지면 따로 보냅니다.
- 상태 파일(`state.json`, `monitor_state.json`)은 깃허브가 알아서 갱신합니다. 손대지 마세요.
- 감시는 첫 실행에 알림을 보내지 않습니다(기존 영상·댓글을 새것으로 오인하지 않으려고).
  채널을 나중에 추가해도 그 채널만 한 번 조용히 넘어갑니다. 정상입니다.
- 구글 승인(선택 단계)을 안 한 상태에서 예약 현황·주간·월간은 **조용히 건너뜁니다.**
  실패가 아닙니다. 워크플로를 끄지 마세요.
- 수강생은 윈도우입니다. 파일을 읽고 쓸 때 `encoding="utf-8"`을 빼지 마세요.
  화면에 찍는 글자에 이모지를 넣지 마세요(윈도우 콘솔에서 죽습니다).
